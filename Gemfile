ruby '1.8.7'
source 'https://rubygems.org'

# These gems are only needed for our integration tests, which use
# Ruby to control the process-pst executable and test its output.
gem 'rspec', '1.3.0'
gem 'assert2', '0.5.5'
gem 'nokogiri', '~> 1.4.2' # for assert2
gem 'mail', '2.2.5'

# The latest versions of these dependencies are no longer compatible with
# Ruby 1.8.7.
gem 'activesupport', '~> 3.0'
gem 'mime-types', '~> 1.0'
